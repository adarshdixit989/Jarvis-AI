const $=id=>document.getElementById(id);const messages=$("messages");function add(role,text){const d=document.createElement("div");d.className="msg "+role;d.innerHTML="<b>"+(role==="user"?"YOU":"JARVIS")+"</b><p>"+escapeHtml(text)+"</p>";messages.appendChild(d);messages.scrollTop=messages.scrollHeight}function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}function respond(raw){const c=raw.toLowerCase().trim();if(c.includes("time"))return "The current browser time is "+new Date().toLocaleTimeString();if(c.includes("date"))return "Today is "+new Date().toLocaleDateString(undefined,{weekday:"long",year:"numeric",month:"long",day:"numeric"})+".";if(c.includes("open youtube")){window.open("https://youtube.com","_blank");return"Opening YouTube in a new tab."}if(c.startsWith("search ")){const q=raw.slice(7);window.open("https://www.google.com/search?q="+encodeURIComponent(q),"_blank");return"Searching the web for "+q+"."}if(c.includes("hello")||c.includes("hi jarvis"))return"Hello. All dashboard systems are operational.";if(c.includes("help"))return"Try time, date, search <topic>, or open YouTube. The full Python assistant adds OS automation and LLM features.";return"I received your command. For full desktop automation, run the Python JARVIS engine from this repository."}function run(v){if(!v.trim())return;add("user",v);setTimeout(()=>add("jarvis",respond(v)),250)}$("commandForm").addEventListener("submit",e=>{e.preventDefault();run($("command").value);$("command").value=""});setInterval(()=>$("clock").textContent=new Date().toLocaleTimeString(),1000);let rec=null;if("SpeechRecognition"in window||"webkitSpeechRecognition"in window){const R=window.SpeechRecognition||window.webkitSpeechRecognition;rec=new R();rec.lang="en-IN";rec.interimResults=false;rec.onstart=()=>{$("voiceState").textContent="LISTENING";$("mic").querySelector("span").textContent="Listening…"};rec.onend=()=>{$("voiceState").textContent="READY";$("mic").querySelector("span").textContent="Speak to JARVIS"};rec.onresult=e=>run(e.results[0][0].transcript)}else $("voiceState").textContent="NOT SUPPORTED";$("mic").onclick=()=>{if(rec)rec.start();else alert("Voice recognition is not supported in this browser. Try Chrome or Edge.")};